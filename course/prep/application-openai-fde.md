# Application packet — OpenAI · Forward Deployed Engineer (FDE)

*One page. Lead with senior end-to-end ownership, full-stack, and eval-driven roadmap influence.
Fill the «placeholders». JD: [`../target-jds/openai-fde.md`](../target-jds/openai-fde.md).*

## Positioning (the one line)
> A senior engineer who **owns a deployment end-to-end** — discovery → scoping → system design →
> build → production rollout — and measures success by **adoption and eval-driven feedback**, not a
> shipped ticket.

## 30-second pitch
«X»+ years shipping systems, «Y» of them customer-facing / in ambiguous, fast-moving contexts. I
scope hard, sequence delivery, and **make the scope/speed/quality trade-off out loud**. I write
**production code front and back** (Python + JS), and I run **eval-driven loops** — I instrument
adoption and feed the signal back to change the roadmap. I contribute in the code whenever momentum
depends on it.

## Proof — their bar → my artifact (all live / verifiable)
| OpenAI asks for | I can show |
|---|---|
| **End-to-end to production** | Owned discovery→build→rollout; **3 live apps** on Vercel, verified in prod |
| **Frontend + backend** | Web cockpits (frontend) + serverless APIs (backend), Python/JS |
| **Eval-driven feedback → roadmap** | `calibration.py` (predicted vs actual outcomes) + eval gates in CI |
| **Scope / speed / quality trade-offs** | staffing-brain (optimal assignment + at-risk flags), a real prioritization engine |
| **Ambiguity → the smallest win** | delta-discovery-protocol → site survey → smallest end-to-end slice |
| **Model behavior → product experience** | agentic apps that degrade gracefully + refuse when ungrounded |

Repo: `github.com/wjlgatech/FDE-os` · live: `portfolio-trust.vercel.app`

## Why OpenAI (specific)
The FDE thesis is that **95% of enterprise AI stalls on approach, not model** — the gap is
deployment, not intelligence. That's the work I optimize for: turn a frontier model into a system a
customer actually adopts, and carry the field signal back to Research/Product. I want to own that
loop end-to-end at frontier scale.

## Cover note (paste-able, ≤120 words)
> I own AI deployments end-to-end and measure them by adoption, not delivery. Recent public work: «a
> live agentic app» shipped front-to-back on Vercel, a calibration loop that validates predictions
> against real outcomes, and an eval gate that blocks anything that can't prove it works. I scope
> hard, make the speed/quality trade-off explicit, and code when momentum needs it. I'd like to turn
> your research breakthroughs into production systems inside your most strategic customers. Running
> artifacts + repo: «link».

## Before you hit send
- [ ] Frame the **5+ yrs / seniority** with one "scoped-and-shipped-under-ambiguity" story that names the trade-offs you made.
- [ ] Show **full-stack** explicitly (a frontend + a backend you deployed).
- [ ] Lead with an **eval-driven** result (a number the loop moved).
- [ ] Portfolio carries **observed + attested** evidence — get **2 vouches** (`portfolio-trust.vercel.app`).
