# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# OS / editor
.DS_Store
*.swp

# Local env / secrets (never commit the Kit form key or provider creds)
.env
.env.local
.gstack/

# Private strategy / meeting notes — NEVER commit to this public repo
private/

# OpenSpace skill-identity sidecars (local tracking, not repo content)
.skill_id

# OpenSpace local artifacts (runner state, not repo content)
.openspace/
logs/

# take-home run artifacts (regenerable; the eval gate is the receipt)
take-home/*/runs/
